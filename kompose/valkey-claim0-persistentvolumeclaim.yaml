apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: valkey-claim0
  name: valkey-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
