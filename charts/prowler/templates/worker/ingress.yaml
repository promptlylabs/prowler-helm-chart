{{- if .Values.worker.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "prowler.fullname" . }}-worker
  labels:
    {{- include "prowler.labels" . | nindent 4 }}
  {{- with .Values.worker.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.worker.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.worker.ingress.tls }}
  tls:
    {{- range .Values.worker.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.worker.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "prowler.fullname" $ }}-worker
                port:
                  number: {{ $.Values.worker.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
