{{- if .Values.worker_beat.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "prowler.fullname" . }}-worker_beat
  labels:
    {{- include "prowler.labels" . | nindent 4 }}
  {{- with .Values.worker_beat.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.worker_beat.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.worker_beat.ingress.tls }}
  tls:
    {{- range .Values.worker_beat.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.worker_beat.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "prowler.fullname" $ }}-worker_beat
                port:
                  number: {{ $.Values.worker_beat.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
